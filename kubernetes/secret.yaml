apiVersion: v1
kind: Secret
metadata:
  name: agentfx-secrets
  namespace: agentfx
type: Opaque
stringData:
  # Replace with base64 encoded or raw string values in deployment pipeline
  ENCRYPTION_KEY: "meridian-default-vault-passphrase-2026"
  CIRCLE_API_KEY: "sandbox_api_key_placeholder"
  DEEPSEEK_API_KEY: "deepseek_api_key_placeholder"
